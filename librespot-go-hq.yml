device_name: "{{NAME}} HQ"
device_type: "speaker"

audio_backend: "pipe"
audio_output_pipe: "/dev/stdout"
audio_output_pipe_format: "s32le"

bitrate: 320

server:
    enabled: true
    port: 24878

credentials:
    type: zeroconf
    zeroconf:
        persist_credentials: true
